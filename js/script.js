// to compile orger into a sentence
document.getElementById("sentence").innerHTML = 5 + 6;

// Delivery alert and collect location
function deliveryAlert() {
  var delivery = confirm ("Would you like Home Delivery? It's 100/= only!!!");
if (delivery = true) {
  var location = prompt ("Enter your location here");
}
}
